version: 2

models:
  - name: example_reporting_model
    description: "An example reporting layer model - business-level aggregations and insights (view)"
    config:
      materialized: view
    columns:
      - name: id
        description: "Primary key"
        tests:
          - unique
          - not_null